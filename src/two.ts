/*
Given a sorted array of integers "haystack" and an integer target "needle", return true if the target exists in the array, and false otherwise.
The search method must be implement manually.

*** EXAMPLE 1: ***
Input:
arr = [1, 3, 5, 7, 9], target = 5
Output:
true
*/

export function search(
    haystack: number[],
    needle: number,
): boolean {
}